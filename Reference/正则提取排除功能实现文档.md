# 正则提取/排除功能实现文档

## 目录

1. [功能概述](#功能概述)
2. [架构设计](#架构设计)
3. [核心模块详解](#核心模块详解)
   - [tagExtractor.js - 核心提取引擎](#tagextractorjs---核心提取引擎)
   - [contentFilter.js - 内容过滤工具](#contentfilterjs---内容过滤工具)
   - [tagScanner.js - 标签扫描器](#tagscannerjs---标签扫描器)
   - [tagParser.js - 标签解析器](#tagparserjs---标签解析器)
4. [UI组件详解](#ui组件详解)
   - [TagRulesEditor.js - 规则编辑器](#tagruleseditorjs---规则编辑器)
   - [TagUI.js - 标签UI管理](#taguijs---标签ui管理)
   - [TagPresetManager.js - 预设管理](#tagpresetmanagerjs---预设管理)
5. [数据流与处理流程](#数据流与处理流程)
6. [规则类型详解](#规则类型详解)
7. [集成点分析](#集成点分析)
8. [性能优化策略](#性能优化策略)
9. [错误处理机制](#错误处理机制)
10. [扩展指南](#扩展指南)

---

## 功能概述

Vectors Enhanced 扩展的正则提取/排除功能提供了一个强大的、基于规则的内容处理系统。该系统允许用户通过直观的图形界面配置规则，精确地从聊天消息中提取或排除特定内容进行向量化。

### 核心特性

- **四种规则类型**：包含、排除、正则包含、正则排除
- **固定的处理顺序**：先排除后提取，确保结果一致性
- **多种标签格式支持**：HTML标签、花括号格式、复杂嵌套标签
- **预设管理系统**：保存、加载、切换不同的规则配置
- **智能标签扫描**：自动发现文本中存在的标签

---

## 架构设计

### 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                      UI Layer (表现层)                       │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │ TagRulesEditor  │ │     TagUI       │ │TagPresetManager ││
│  │   (规则编辑器)   │ │  (标签UI管理)    │ │   (预设管理)     ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Core Layer (核心层)                       │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐│
│  │  tagExtractor   │ │  contentFilter  │ │   tagScanner    ││
│  │   (提取引擎)     │ │  (过滤工具)      │ │   (扫描器)       ││
│  └─────────────────┘ └─────────────────┘ └─────────────────┘│
│  ┌─────────────────┐                                         │
│  │   tagParser     │                                         │
│  │   (解析器)       │                                         │
│  └─────────────────┘                                         │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                Integration Layer (集成层)                    │
│  ┌─────────────────┐ ┌─────────────────┐                    │
│  │  ChatExtractor  │ │  FileExtractor  │                    │
│  │  (聊天提取器)    │ │  (文件提取器)    │                    │
│  └─────────────────┘ └─────────────────┘                    │
└─────────────────────────────────────────────────────────────┘
```

### 模块依赖关系

```
ChatExtractor
     │
     ├──→ tagExtractor.extractTagContent()
     │          │
     │          ├──→ contentFilter.escapeRegex()
     │          │
     │          └──→ contentFilter.shouldSkipContent()
     │
     └──→ chatUtils.getMessages()

TagRulesEditor
     │
     └──→ extension_settings.vectors_enhanced

TagUI
     │
     ├──→ tagScanner.scanTextForTags()
     │          │
     │          └──→ contentFilter.isValidTagName()
     │
     └──→ TagRulesEditor.renderTagRulesUI()

TagPresetManager
     │
     ├──→ extension_settings.vectors_enhanced.tagPresets
     │
     └──→ TagRulesEditor.renderTagRulesUI()
```

---

## 核心模块详解

### tagExtractor.js - 核心提取引擎

**文件路径**: `src/utils/tagExtractor.js`

这是整个正则提取/排除功能的核心模块，负责执行所有的内容提取和过滤操作。

#### 主函数: `extractTagContent(text, rules, blacklist)`

```javascript
/**
 * 根据规则集从文本中提取和过滤内容
 * @param {string} text - 输入文本
 * @param {Array<object>} rules - 规则数组
 * @param {Array<string>} blacklist - 黑名单关键词数组
 * @returns {string} 处理后的内容
 */
export function extractTagContent(text, rules, blacklist = [])
```

**处理流程**（三阶段架构）：

```
┌────────────────────────────────────────────────────────────────┐
│                    Phase 1: 全局块级排除                        │
│                                                                │
│  规则类型: exclude (排除)                                       │
│  操作: 从原始文本中移除匹配的标签块                              │
│  正则模式: <tagName[\s\S]*?</tagName>                          │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                    Phase 2: 内容提取                           │
│                                                                │
│  规则类型: include (包含) / regex_include (正则包含)            │
│  操作: 从"净化后"的文本中提取需要的内容                          │
│  - include: 调用 extractSimpleTag() + extractCurlyBraceTag()  │
│  - regex_include: 使用正则表达式的第一个捕获组                  │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                 Phase 3: 内部清理与黑名单过滤                   │
│                                                                │
│  规则类型: regex_exclude (正则排除)                             │
│  操作: 对提取的内容进行进一步清理                                │
│                                                                │
│  黑名单检查: shouldSkipContent()                               │
│  操作: 过滤包含黑名单关键词的内容块                              │
└────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌────────────────────────────────────────────────────────────────┐
│                    最终输出                                     │
│                                                                │
│  - 合并所有提取的内容（用双换行分隔）                            │
│  - 清理多余空白                                                 │
│  - 返回处理后的文本                                             │
└────────────────────────────────────────────────────────────────┘
```

**实现细节**：

```javascript
export function extractTagContent(text, rules, blacklist = []) {
    if (!rules || rules.length === 0) {
        return text;
    }

    // 规则分类
    const blockExcludeRules = rules.filter(rule => rule.type === 'exclude' && rule.enabled);
    const includeRules = rules.filter(rule => (rule.type === 'include' || rule.type === 'regex_include') && rule.enabled);
    const cleanupRules = rules.filter(rule => rule.type === 'regex_exclude' && rule.enabled);

    let workingText = text;

    // Phase 1: Global Block-Level Exclusion
    for (const rule of blockExcludeRules) {
        try {
            const tagRegex = new RegExp(
                `<${escapeRegex(rule.value)}(?:\\s[^>]*)?>[\\s\\S]*?<\\/${escapeRegex(rule.value)}>`, 
                'gi'
            );
            workingText = workingText.replace(tagRegex, '');
        } catch (error) {
            console.error(`Error applying block exclusion rule:`, { rule, error });
        }
    }

    // Phase 2: Content Extraction
    let extractedContents = [];
    if (includeRules.length > 0) {
        for (const rule of includeRules) {
            let results = [];
            try {
                if (rule.type === 'include') {
                    results.push(...extractSimpleTag(workingText, rule.value));
                    results.push(...extractCurlyBraceTag(workingText, rule.value));
                } else if (rule.type === 'regex_include') {
                    const regex = new RegExp(rule.value, 'gi');
                    const matches = [...workingText.matchAll(regex)];
                    matches.forEach(match => {
                        if (match[1]) results.push(match[1]);
                    });
                }
            } catch (error) {
                console.error(`Error applying inclusion rule:`, { rule, error });
            }
            results.forEach(content => extractedContents.push(content.trim()));
        }
    } else {
        // 无包含规则时，使用全部文本
        extractedContents.push(workingText);
    }

    // Phase 3: Inner Content Cleanup & Blacklist Filtering
    let finalContents = [];
    for (let contentBlock of extractedContents) {
        // 应用 regex_exclude 清理规则
        for (const rule of cleanupRules) {
            try {
                const regex = new RegExp(rule.value, 'gi');
                contentBlock = contentBlock.replace(regex, '');
            } catch (error) {
                console.error(`Error applying cleanup rule:`, { rule, error });
            }
        }

        // 应用黑名单
        if (!shouldSkipContent(contentBlock, blacklist)) {
            finalContents.push(contentBlock);
        }
    }

    // 合并并清理
    const joinedContent = finalContents.join('\n\n');
    return joinedContent
        .replace(/\n\s*\n\s*\n/g, '\n\n')  // 最多保留双换行
        .replace(/^\s+|\s+$/g, '')          // 去除首尾空白
        .trim();
}
```

#### 辅助提取函数

##### `extractSimpleTag(text, tag)` - 简单标签提取

```javascript
/**
 * 使用简单标签名提取内容
 * @param {string} text - 搜索文本
 * @param {string} tag - 简单标签名，如 "content" 或 "thinking"
 * @returns {string[]} 提取的内容数组
 * 
 * 示例: tag = "content"
 * 匹配: <content>提取这部分</content>
 * 返回: ["提取这部分"]
 */
export function extractSimpleTag(text, tag) {
    const extractedContent = [];
    const regex = new RegExp(`<${tag}>([\\s\\S]*?)<\\/${tag}>`, 'gi');
    const matches = [...text.matchAll(regex)];

    matches.forEach(match => {
        if (match[1]) {
            extractedContent.push(match[1].trim());
        }
    });

    // 未闭合标签警告
    const openTags = (text.match(new RegExp(`<${tag}>`, 'gi')) || []).length;
    const closeTags = (text.match(new RegExp(`<\\/${tag}>`, 'gi')) || []).length;
    if (openTags > closeTags) {
        console.warn(`警告: 发现 ${openTags - closeTags} 个未闭合的 <${tag}> 标签`);
    }

    return extractedContent;
}
```

##### `extractCurlyBraceTag(text, tag)` - 花括号格式提取

```javascript
/**
 * 使用花括号管道格式提取内容
 * @param {string} text - 搜索文本
 * @param {string} tag - 标签名，如 "outputstory" 对应格式 {outputstory|content}
 * @returns {string[]} 提取的内容数组
 * 
 * 示例: tag = "story"
 * 匹配: {story|这部分内容}
 * 返回: ["这部分内容"]
 * 
 * 特性: 支持嵌套花括号，使用计数器确保正确匹配
 */
export function extractCurlyBraceTag(text, tag) {
    const extractedContent = [];
    const escapedTag = escapeRegex(tag);

    // 查找所有起始位置
    const startPattern = new RegExp(`\\{${escapedTag}\\|`, 'gi');
    let match;

    while ((match = startPattern.exec(text)) !== null) {
        const startPos = match.index;
        const contentStart = startPos + match[0].length;

        // 使用花括号计数器处理嵌套
        let braceCount = 1;
        let pos = contentStart;

        while (pos < text.length && braceCount > 0) {
            if (text[pos] === '{') {
                braceCount++;
            } else if (text[pos] === '}') {
                braceCount--;
            }
            pos++;
        }

        if (braceCount === 0) {
            const content = text.substring(contentStart, pos - 1);
            if (content.trim()) {
                extractedContent.push(content.trim());
            }
        }

        // 继续搜索
        startPattern.lastIndex = startPos + 1;
    }

    return extractedContent;
}
```

##### `extractComplexTag(text, tag)` - 复杂标签配置

```javascript
/**
 * 使用复杂标签配置提取内容
 * @param {string} text - 搜索文本
 * @param {string} tag - 复杂标签配置，如 "<details><summary>摘要</summary>,</details>"
 * @returns {string[]} 提取的内容数组
 * 
 * 格式说明: 用逗号分隔起始模式和结束模式
 * 示例: tag = "<details><summary>摘要</summary>,</details>"
 *       起始模式: <details><summary>摘要</summary>
 *       结束模式: </details>
 * 匹配: <details><summary>摘要</summary>内容</details>
 * 返回: ["内容"]
 */
export function extractComplexTag(text, tag) {
    const parts = tag.split(',');
    if (parts.length !== 2) {
        throw new Error(`复杂标签配置格式错误，应该包含一个逗号: ${tag}`);
    }

    const startPattern = parts[0].trim();
    const endPattern = parts[1].trim();

    // 提取结束标签名
    const endTagMatch = endPattern.match(/<\/(\w+)>/);
    if (!endTagMatch) {
        throw new Error(`无法解析结束标签: ${endPattern}`);
    }
    const endTagName = endTagMatch[1];

    // 构建匹配正则
    const regex = new RegExp(`${escapeRegex(startPattern)}([\\s\\S]*?)<\\/${endTagName}>`, 'gi');

    const extractedContent = [];
    const matches = [...text.matchAll(regex)];

    matches.forEach(match => {
        if (match[1]) {
            extractedContent.push(match[1].trim());
        }
    });

    return extractedContent;
}
```

##### `extractHtmlFormatTag(text, tag)` - HTML格式标签

```javascript
/**
 * 使用HTML格式标签提取内容
 * @param {string} text - 搜索文本
 * @param {string} tag - HTML格式标签，如 "<content></content>"
 * @returns {string[]} 提取的内容数组
 * 
 * 特性: 支持标签属性
 * 示例: tag = "<content>"
 * 匹配: <content class="main">内容</content>
 * 返回: ["内容"]
 */
export function extractHtmlFormatTag(text, tag) {
    // 提取标签名，处理可能的属性
    const tagMatch = tag.match(/<(\w+)(?:\s[^>]*)?>/);
    if (!tagMatch) {
        throw new Error(`无法解析HTML格式标签: ${tag}`);
    }
    const tagName = tagMatch[1];

    const extractedContent = [];
    const regex = new RegExp(`<${tagName}(?:\\s[^>]*)?>([\\s\\S]*?)<\\/${tagName}>`, 'gi');
    const matches = [...text.matchAll(regex);

    matches.forEach(match => {
        if (match[1]) {
            extractedContent.push(match[1].trim());
        }
    });

    // 未闭合标签检查
    const openTags = (text.match(new RegExp(`<${tagName}(?:\\s[^>]*)?>`, 'gi')) || []).length;
    const closeTags = (text.match(new RegExp(`<\\/${tagName}>`, 'gi')) || []).length;

    if (openTags > closeTags) {
        console.warn(`警告: 发现 ${openTags - closeTags} 个未闭合的 <${tagName}> 标签`);
    }

    return extractedContent;
}
```

---

### contentFilter.js - 内容过滤工具

**文件路径**: `src/utils/contentFilter.js`

提供内容过滤和正则处理的基础工具函数。

#### `shouldSkipContent(text, blacklist)` - 黑名单检查

```javascript
/**
 * 检查内容是否应该被跳过（基于黑名单）
 * @param {string} text - 要检查的内容
 * @param {string[]} blacklist - 黑名单关键词数组
 * @returns {boolean} 如果应该跳过则返回 true
 * 
 * 特性: 不区分大小写的子串匹配
 */
export function shouldSkipContent(text, blacklist) {
    if (!blacklist || blacklist.length === 0) return false;

    const lowerText = text.toLowerCase();
    return blacklist.some(keyword => {
        const lowerKeyword = keyword.trim().toLowerCase();
        return lowerKeyword && lowerText.includes(lowerKeyword);
    });
}
```

#### `escapeRegex(str)` - 正则转义

```javascript
/**
 * 转义字符串中的正则特殊字符
 * @param {string} str - 要转义的字符串
 * @returns {string} 转义后的字符串
 * 
 * 转义字符: . * + ? ^ $ { } ( ) | [ ] \ 
 */
export function escapeRegex(str) {
    return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
```

#### `isValidTagName(tagName)` - 标签名验证

```javascript
/**
 * 验证标签名是否适合提取
 * @param {string} tagName - 要验证的标签名
 * @returns {boolean} 如果标签名有效则返回 true
 * 
 * 规则:
 * 1. 必须以字母开头
 * 2. 只能包含字母、数字、下划线和连字符
 * 3. 不能是排除的HTML格式标签
 */
export function isValidTagName(tagName) {
    // 排除常见的HTML格式标签
    const excludedTags = [
        'font', 'span', 'div', 'p', 'br', 'hr', 'img', 'a', 'b', 'i', 'u', 's',
        'em', 'strong', 'small', 'big', 'sub', 'sup', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6',
        'table', 'tr', 'td', 'th', 'tbody', 'thead', 'tfoot', 'ul', 'ol', 'li',
        'form', 'input', 'button', 'select', 'option', 'textarea', 'label',
        'script', 'style', 'meta', 'link', 'title', 'head', 'body', 'html'
    ];

    // 必须是字母开头，可包含下划线和连字符
    const validPattern = /^[a-zA-Z][a-zA-Z0-9_-]*$/;

    return validPattern.test(tagName) && !excludedTags.includes(tagName.toLowerCase());
}
```

---

### tagScanner.js - 标签扫描器

**文件路径**: `src/utils/tagScanner.js`

提供高效的标签扫描功能，用于自动发现文本中存在的标签。

#### `scanTextForTags(text, options)` - 文本标签扫描

```javascript
/**
 * 高效扫描大文本中可用的标签
 * @param {string} text - 要扫描的大文本内容（可达数十万字符）
 * @param {object} options - 扫描选项
 * @param {number} options.chunkSize - 文本块大小（默认: 50000）
 * @param {number} options.maxTags - 最大发现标签数（默认: 100）
 * @param {number} options.timeoutMs - 最大处理时间（默认: 5000ms）
 * @returns {Promise<object>} 包含发现标签和性能统计的对象
 */
export async function scanTextForTags(text, options = {})
```

**实现细节**：

```javascript
export async function scanTextForTags(text, options = {}) {
    const startTime = performance.now();
    const {
        chunkSize = 50000,
        maxTags = 100,
        timeoutMs = 5000
    } = options;

    const foundTags = new Set();
    
    // 统一匹配 HTML 标签和花括号格式标签
    // match[1] 用于 <tag>, match[2] 用于 {tag}
    const tagRegex = /<(?:\/|)([a-zA-Z0-9_-]+)(?:[^>]*)>|\{([a-zA-Z0-9_-]+)(?:\||})/g;

    let processedChars = 0;
    let chunkCount = 0;

    // 分块处理
    for (let i = 0; i < text.length; i += chunkSize) {
        const chunk = text.slice(i, Math.min(i + chunkSize, text.length));
        chunkCount++;
        processedChars += chunk.length;

        // 超时检查
        if (performance.now() - startTime > timeoutMs) {
            console.warn(`Tag scanning timed out after ${timeoutMs}ms`);
            break;
        }

        // 执行匹配
        let match;
        while ((match = tagRegex.exec(chunk)) !== null && foundTags.size < maxTags) {
            const tagName = (match[1] || match[2]).toLowerCase();
            if (isValidTagName(tagName)) {
                foundTags.add(tagName);
            }
        }

        if (foundTags.size >= maxTags) break;

        // 每5个块让出主线程
        if (chunkCount % 5 === 0) {
            await new Promise(resolve => setTimeout(resolve, 0));
        }
    }

    const endTime = performance.now();

    return {
        tags: Array.from(foundTags).sort(),
        stats: {
            processingTimeMs: Math.round(endTime - startTime),
            processedChars,
            totalChars: text.length,
            chunkCount,
            tagsFound: foundTags.size
        }
    };
}
```

#### `generateTagSuggestions(scanResult, limit)` - 生成标签建议

```javascript
/**
 * 基于扫描结果生成标签建议
 * @param {object} scanResult - scanTextForTags 的结果
 * @param {number} limit - 最大建议数量（默认: 25）
 * @returns {object} 包含建议数组和详细统计的对象
 */
export function generateTagSuggestions(scanResult, limit = 25) {
    const suggestions = scanResult.tags.slice(0, limit);

    return {
        suggestions: suggestions,
        stats: {
            totalFound: scanResult.stats.tagsFound,
            finalCount: suggestions.length
        }
    };
}
```

---

### tagParser.js - 标签解析器

**文件路径**: `src/utils/tagParser.js`

提供旧版命令式标签配置的解析功能（保留向后兼容）。

#### `parseTagWithExclusions(tagConfig)` - 解析带排除的标签配置

```javascript
/**
 * 解析可能包含排除项的标签配置字符串
 * 格式: "include,tags - exclude,tags"
 * @param {string} tagConfig - 标签配置字符串
 * @returns {{mainTag: string, excludeTags: string[]}} 主标签和排除标签数组
 * 
 * 示例:
 * "content - thinking,analysis" => { mainTag: "content", excludeTags: ["thinking", "analysis"] }
 */
export function parseTagWithExclusions(tagConfig) {
    if (!tagConfig || typeof tagConfig !== 'string') {
        return { mainTag: '', excludeTags: [] };
    }

    const parts = tagConfig.split(' - ');
    const mainTag = (parts[0] || '').trim();
    const excludePart = (parts[1] || '').trim();

    const excludeTags = excludePart 
        ? excludePart.split(',').map(t => t.trim()).filter(Boolean) 
        : [];

    return { mainTag, excludeTags };
}
```

#### `removeExcludedTags(content, excludeTags)` - 移除排除标签

```javascript
/**
 * 从内容中移除指定排除标签内的内容
 * @param {string} content - 原始内容
 * @param {string[]} excludeTags - 要移除的标签数组
 * @returns {string} 移除后的内容
 * 
 * 注意: 此函数设计用于处理简单的非嵌套标签
 */
export function removeExcludedTags(content, excludeTags) {
    if (!content || !excludeTags || excludeTags.length === 0) {
        return content;
    }

    let processedContent = content;
    for (const tag of excludeTags) {
        const regex = new RegExp(`<${tag}>[\\s\\S]*?<\\/${tag}>`, 'g');
        processedContent = processedContent.replace(regex, '');
    }

    return processedContent;
}
```

---

## UI组件详解

### TagRulesEditor.js - 规则编辑器

**文件路径**: `src/ui/components/TagRulesEditor.js`

负责渲染和管理标签规则编辑界面。

#### `renderTagRulesUI()` - 渲染规则UI

```javascript
/**
 * 在设置面板中渲染标签规则UI
 */
export function renderTagRulesUI() {
    const settings = extension_settings.vectors_enhanced;
    const editor = $('#vectors_enhanced_rules_editor');
    editor.empty();

    // 确保规则数组存在
    if (!settings.selected_content.chat.tag_rules || 
        !Array.isArray(settings.selected_content.chat.tag_rules)) {
        settings.selected_content.chat.tag_rules = [];
    }
    const rules = settings.selected_content.chat.tag_rules;

    // 空状态提示
    if (rules.length === 0) {
        editor.append('<div class="text-muted" style="margin: 0.5rem 0;">没有定义任何提取规则。</div>');
    }

    // 渲染每条规则
    rules.forEach((rule, index) => {
        const ruleHtml = `
            <div class="vector-enhanced-rule-item flex-container alignItemsCenter" 
                 data-index="${index}" 
                 style="margin-bottom: 0.5rem; gap: 0.5rem;">
                <select class="rule-type text_pole widthUnset" style="flex: 2;">
                    <option value="include" ${rule.type === 'include' ? 'selected' : ''}>包含</option>
                    <option value="regex_include" ${rule.type === 'regex_include' ? 'selected' : ''}>正则包含</option>
                    <option value="exclude" ${rule.type === 'exclude' ? 'selected' : ''}>排除</option>
                    <option value="regex_exclude" ${rule.type === 'regex_exclude' ? 'selected' : ''}>正则排除</option>
                </select>
                <input type="text" class="rule-value text_pole" style="flex: 5;" 
                       placeholder="标签名或/表达式/" 
                       value="${rule.value || ''}">
                <label class="checkbox_label" style="flex: 1; white-space: nowrap;">
                    <input type="checkbox" class="rule-enabled" ${rule.enabled ? 'checked' : ''}>
                    <span>启用</span>
                </label>
                <button class="menu_button menu_button_icon rule-delete" title="删除规则">
                    <i class="fa-solid fa-trash"></i>
                </button>
            </div>
        `;
        editor.append(ruleHtml);
    });

    // 绑定事件处理器
    bindEventHandlers(editor, settings);
}
```

#### 事件处理

```javascript
// 规则变更事件
editor.on('change', '.rule-type, .rule-value, .rule-enabled', function() {
    const ruleDiv = $(this).closest('.vector-enhanced-rule-item');
    const index = ruleDiv.data('index');
    const rule = settings.selected_content.chat.tag_rules[index];

    if ($(this).hasClass('rule-type')) {
        rule.type = $(this).val();
    }
    if ($(this).hasClass('rule-value')) {
        rule.value = $(this).val();
    }
    if ($(this).hasClass('rule-enabled')) {
        rule.enabled = $(this).is(':checked');
    }

    Object.assign(extension_settings.vectors_enhanced, { selected_content: settings.selected_content });
    saveSettingsDebounced();
});

// 删除规则事件
editor.on('click', '.rule-delete', function() {
    const index = $(this).closest('.vector-enhanced-rule-item').data('index');
    settings.selected_content.chat.tag_rules.splice(index, 1);
    saveSettingsDebounced();
    renderTagRulesUI(); // 重新渲染
});
```

---

### TagUI.js - 标签UI管理

**文件路径**: `src/ui/components/TagUI.js`

管理标签建议显示、示例文档展示等UI交互。

#### 核心功能

##### `displayTagSuggestions(suggestions, scanStats)` - 显示标签建议

```javascript
/**
 * 在UI中显示标签建议
 * @param {string[]} suggestions - 标签建议数组
 * @param {object} scanStats - 扫描性能统计
 */
const displayTagSuggestions = (suggestions, scanStats) => {
    const container = $('#vectors_enhanced_tag_suggestions');
    const tagList = $('#vectors_enhanced_tag_list');
    const statsSpan = $('#vectors_tag_scan_stats');

    // 更新统计显示
    const statsText = `${suggestions.length} 个标签，${scanStats.processingTimeMs}ms`;
    statsSpan.text(statsText);

    // 清空之前的建议
    tagList.empty();

    if (suggestions.length === 0) {
        container.hide();
        return;
    }

    // 创建标签按钮
    suggestions.forEach(tag => {
        const tagBtn = $(`<button class="menu_button tag-suggestion-btn" title="点击添加到标签提取框"></button>`);
        tagBtn.text(tag); // 使用 .text() 防止HTML解析

        tagBtn.on('click', () => {
            addScannedTagAsRule(tag);
        });

        tagList.append(tagBtn);
    });

    container.show();
};
```

##### `addScannedTagAsRule(tag)` - 添加扫描到的标签为规则

```javascript
/**
 * 将扫描到的标签添加为新的'包含'规则
 * @param {string} tag - 要添加的标签名
 */
function addScannedTagAsRule(tag) {
    const settings = extension_settings.vectors_enhanced;
    
    // 确保规则数组存在
    if (!settings.selected_content.chat.tag_rules) {
        settings.selected_content.chat.tag_rules = [];
    }

    // 检查是否已存在
    const alreadyExists = settings.selected_content.chat.tag_rules.some(
        rule => rule.type === 'include' && rule.value === tag
    );

    if (alreadyExists) {
        toastr.info(`规则 "包含: ${tag}" 已存在。`);
        return;
    }

    // 添加新规则
    const newRule = {
        type: 'include',
        value: tag,
        enabled: true,
    };
    settings.selected_content.chat.tag_rules.push(newRule);

    saveSettingsDebounced();
    renderTagRulesUI();

    toastr.success(`已添加新规则: "包含: ${tag}"`);
}
```

##### `showTagExamples()` - 显示标签提取示例

```javascript
/**
 * 显示标签提取示例文档（从Markdown转换为格式化的HTML弹窗）
 */
async function showTagExamples() {
    try {
        // 读取示例文件
        const response = await fetch('/scripts/extensions/third-party/vectors-enhanced/标签提取示例.md');
        const rawContent = await response.text();
        
        // Markdown 到 HTML 的转换（带样式）
        const htmlContent = convertMarkdownToStyledHtml(rawContent);
        
        // 显示弹窗
        await callGenericPopup(htmlContent, POPUP_TYPE.TEXT, '', {
            okButton: '关闭',
            wide: true,
            large: true,
        });
    } catch (error) {
        console.error('显示标签示例失败:', error);
        toastr.error('无法加载标签示例: ' + error.message);
    }
}
```

---

### TagPresetManager.js - 预设管理

**文件路径**: `src/ui/components/TagPresetManager.js`

提供标签规则的预设保存、加载和管理功能。

#### 类结构

```javascript
export class TagPresetManager {
    constructor() {
        this.initializePresets();
    }

    /**
     * 初始化预设存储
     */
    initializePresets() {
        if (!extension_settings.vectors_enhanced) {
            extension_settings.vectors_enhanced = {};
        }

        if (!extension_settings.vectors_enhanced.tagPresets) {
            extension_settings.vectors_enhanced.tagPresets = {
                presets: {},        // 预设集合
                activePresetId: null // 当前激活的预设ID
            };
            saveSettingsDebounced();
        }
    }
}
```

#### 核心方法

##### `saveCurrentAsPreset()` - 保存当前配置为预设

```javascript
async saveCurrentAsPreset() {
    // 弹出输入框获取预设名称
    const presetName = await callGenericPopup(
        '输入预设名称：',
        POPUP_TYPE.INPUT,
        '',
        { okButton: '保存', cancelButton: '取消' }
    );

    if (!presetName || !presetName.trim()) return;

    // 生成唯一ID
    const presetId = `preset_${Date.now()}`;
    
    // 获取当前规则
    const currentRules = extension_settings.vectors_enhanced.selected_content.chat.tag_rules || [];
    const applyToFirstMessage = extension_settings.vectors_enhanced.selected_content.chat.apply_tags_to_first_message || false;

    // 保存预设
    extension_settings.vectors_enhanced.tagPresets.presets[presetId] = {
        id: presetId,
        name: presetName.trim(),
        rules: JSON.parse(JSON.stringify(currentRules)), // 深拷贝
        applyToFirstMessage: applyToFirstMessage,
        createdAt: Date.now()
    };

    extension_settings.vectors_enhanced.tagPresets.activePresetId = presetId;
    saveSettingsDebounced();

    this.updatePresetSelector();
    toastr.success(`预设 "${presetName}" 已保存`);
}
```

##### `loadPreset(presetId)` - 加载预设

```javascript
loadPreset(presetId) {
    if (!presetId) {
        // 加载默认（空）规则
        extension_settings.vectors_enhanced.selected_content.chat.tag_rules = [];
        extension_settings.vectors_enhanced.selected_content.chat.apply_tags_to_first_message = false;
        extension_settings.vectors_enhanced.tagPresets.activePresetId = null;
    } else {
        const preset = extension_settings.vectors_enhanced.tagPresets.presets[presetId];
        if (!preset) {
            toastr.error('预设不存在');
            return;
        }

        // 深拷贝规则
        extension_settings.vectors_enhanced.selected_content.chat.tag_rules = 
            JSON.parse(JSON.stringify(preset.rules));
        extension_settings.vectors_enhanced.selected_content.chat.apply_tags_to_first_message = 
            preset.applyToFirstMessage;
        extension_settings.vectors_enhanced.tagPresets.activePresetId = presetId;
    }

    saveSettingsDebounced();
    renderTagRulesUI();

    // 更新复选框状态
    $('#vectors_enhanced_apply_tags_to_first_message').prop('checked',
        extension_settings.vectors_enhanced.selected_content.chat.apply_tags_to_first_message);
}
```

##### `renamePreset(presetId)` - 重命名预设

```javascript
async renamePreset(presetId) {
    const preset = extension_settings.vectors_enhanced.tagPresets.presets[presetId];
    if (!preset) {
        toastr.error('预设不存在');
        return;
    }

    const newName = await callGenericPopup(
        '输入新的预设名称：',
        POPUP_TYPE.INPUT,
        preset.name,
        { okButton: '确定', cancelButton: '取消' }
    );

    if (!newName || !newName.trim() || newName.trim() === preset.name) return;

    preset.name = newName.trim();
    saveSettingsDebounced();
    this.updatePresetSelector();
    toastr.success(`预设已重命名为 "${newName}"`);
}
```

---

## 数据流与处理流程

### 完整数据流图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户操作                                        │
│                                                                             │
│  1. 打开设置面板 → 展开内容选择 → 配置标签规则                                │
│  2. 点击"扫描标签" → 发现可用标签 → 点击添加                                  │
│  3. 保存预设以便复用                                                        │
│  4. 点击"向量化"或"预览"                                                    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           规则配置存储                                       │
│                                                                             │
│  extension_settings.vectors_enhanced.selected_content.chat.tag_rules = [   │
│      { type: 'exclude', value: 'thinking', enabled: true },                │
│      { type: 'include', value: 'content', enabled: true },                 │
│      { type: 'regex_include', value: '<details>([\\s\\S]*?)</details>',    │
│        enabled: true }                                                      │
│  ]                                                                          │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ChatExtractor.extract()                             │
│                                                                             │
│  1. 获取聊天消息列表                                                         │
│  2. 读取配置规则                                                             │
│  3. 调用 extractTagContent() 处理每条消息                                   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                     extractTagContent() 三阶段处理                          │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ Phase 1: 全局排除                                                    │   │
│  │ • 过滤: type === 'exclude' && enabled                                │   │
│  │ • 操作: 移除 <thinking>...</thinking> 等块                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ Phase 2: 内容提取                                                    │   │
│  │ • 过滤: (type === 'include' || type === 'regex_include') && enabled │   │
│  │ • include: 调用 extractSimpleTag() + extractCurlyBraceTag()         │   │
│  │ • regex_include: 使用捕获组提取                                      │   │
│  │ • 无规则时: 使用全部文本                                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ Phase 3: 清理与黑名单                                                │   │
│  │ • regex_exclude: 对提取内容进行进一步清理                            │   │
│  │ • 黑名单: 过滤包含黑名单关键词的内容                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              输出结果                                        │
│                                                                             │
│  • 合并所有提取的内容（双换行分隔）                                          │
│  • 清理多余空白                                                             │
│  • 返回给调用者（ChatExtractor → 向量化流程）                               │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 规则类型详解

### 规则数据结构

```typescript
interface TagRule {
    type: 'include' | 'exclude' | 'regex_include' | 'regex_exclude';
    value: string;
    enabled: boolean;
}
```

### 四种规则类型对比

| 规则类型 | 执行阶段 | 功能描述 | 正则模式示例 |
|---------|---------|---------|-------------|
| `exclude` | Phase 1 | 块级排除，移除整个标签块 | `<thinking>[\s\S]*?</thinking>` |
| `include` | Phase 2 | 简单标签提取 | `<content>([\s\S]*?)</content>` |
| `regex_include` | Phase 2 | 正则表达式提取（使用捕获组） | 用户自定义，提取第一个捕获组 |
| `regex_exclude` | Phase 3 | 内容清理，对已提取内容进行清理 | 用户自定义 |

### 正则表达式详解

#### exclude 规则的正则构建

```javascript
// 输入: rule.value = "thinking"
// 输出正则: <thinking(?:\s[^>]*)?>[\s\S]*?</thinking>
// 匹配: <thinking>任何内容</thinking> 或 <thinking class="note">任何内容</thinking>

const tagRegex = new RegExp(
    `<${escapeRegex(rule.value)}(?:\\s[^>]*)?>[\\s\\S]*?<\\/${escapeRegex(rule.value)}>`, 
    'gi'
);
```

**模式分解**：
- `<thinking` - 标签开始
- `(?:\s[^>]*)?` - 可选的属性部分（非捕获组）
- `>` - 标签结束符
- `[\s\S]*?` - 任意内容（包括换行），非贪婪
- `</thinking>` - 结束标签
- `gi` 标志 - 全局匹配，不区分大小写

#### include 规则的处理

```javascript
// 同时调用两种提取函数
results.push(...extractSimpleTag(workingText, rule.value));
results.push(...extractCurlyBraceTag(workingText, rule.value));
```

#### regex_include 规则的处理

```javascript
// 使用用户提供的正则表达式
const regex = new RegExp(rule.value, 'gi');
const matches = [...workingText.matchAll(regex)];
matches.forEach(match => {
    if (match[1]) results.push(match[1]); // 提取第一个捕获组
});
```

**重要**: `regex_include` 规则必须包含至少一个捕获组 `(...)`，系统只提取第一个捕获组的内容。

#### regex_exclude 规则的处理

```javascript
// 对每个提取的内容块应用清理
for (const rule of cleanupRules) {
    const regex = new RegExp(rule.value, 'gi');
    contentBlock = contentBlock.replace(regex, '');
}
```

---

## 集成点分析

### ChatExtractor 集成

**文件**: `src/core/extractors/ChatExtractor.js`

```javascript
async extract(source, config = {}) {
    const settings = extension_settings.vectors_enhanced;
    const items = [];

    if (Array.isArray(source)) {
        // 管道模式
        const chatSettings = config || settings.selected_content.chat;
        const rules = chatSettings.tag_rules || [];
        
        source.forEach(item => {
            let extractedText;
            const applyTagsToFirstMessage = chatSettings.apply_tags_to_first_message || false;
            
            // 特殊处理：第0层和用户消息
            if ((item.metadata?.index === 0 && !applyTagsToFirstMessage) || 
                item.metadata?.is_user === true) {
                extractedText = item.text; // 保持原样
            } else {
                // 应用标签提取规则
                extractedText = extractTagContent(
                    item.text, 
                    rules, 
                    settings.content_blacklist || []
                );
            }
            
            items.push({
                text: extractedText,
                metadata: item.metadata,
                type: 'chat'
            });
        });
    }

    return {
        content: items.map(item => item.text).join('\n\n'),
        metadata: { extractorType: 'ChatExtractor', messageCount: items.length }
    };
}
```

### 设置界面集成

**文件**: `settings-modular.html`

```html
<!-- 标签提取规则区域 -->
<div class="flex-container flexFlowColumn m-t-0-5">
    <div class="flex-container alignItemsCenter">
        <label class="flex1 vectors-enhanced-subsection-title">
            <small>标签提取规则</small>
        </label>
        <button id="vectors_enhanced_tag_examples" class="menu_button menu_button_icon">
            <i class="fa-solid fa-lightbulb"></i>
            <span>查看示例</span>
        </button>
    </div>
    
    <!-- 预设选择器 -->
    <div class="tag-preset-management">
        <select id="vectors_enhanced_tag_preset_selector" class="text_pole flex1">
            <option value="">默认规则</option>
        </select>
        <button id="vectors_enhanced_tag_preset_save">保存预设</button>
    </div>
    
    <!-- 第0层处理选项 -->
    <label class="checkbox_label">
        <input id="vectors_enhanced_apply_tags_to_first_message" type="checkbox" />
        <span>对第0层（角色卡）也应用标签提取规则</span>
    </label>
    
    <!-- 规则编辑器容器 -->
    <div id="vectors_enhanced_rules_editor"></div>
    
    <!-- 操作按钮 -->
    <div class="flex-container vectors-tag-actions">
        <button id="vectors_enhanced_add_rule">添加新规则</button>
        <button id="vectors_enhanced_tag_scanner">扫描标签</button>
        <button id="vectors_enhanced_exclude_cot">排除小CoT</button>
        <button id="vectors_enhanced_format_fix">掉格式兼容</button>
    </div>
    
    <!-- 标签建议区域 -->
    <div id="vectors_enhanced_tag_suggestions">
        <div id="vectors_enhanced_tag_list"></div>
    </div>
</div>
```

---

## 性能优化策略

### 1. 标签扫描性能优化

```javascript
// 分块处理大文本
const chunkSize = 50000; // 50KB 每块

// 超时保护
const timeoutMs = 5000; // 最大5秒

// 定期让出主线程
if (chunkCount % 5 === 0) {
    await new Promise(resolve => setTimeout(resolve, 0));
}
```

### 2. 规则处理优化

```javascript
// 规则预处理分类（避免每次循环都过滤）
const blockExcludeRules = rules.filter(rule => rule.type === 'exclude' && rule.enabled);
const includeRules = rules.filter(rule => (rule.type === 'include' || rule.type === 'regex_include') && rule.enabled);
const cleanupRules = rules.filter(rule => rule.type === 'regex_exclude' && rule.enabled);
```

### 3. 正则表达式优化

```javascript
// 使用字符串方法避免不必要的正则
const lowerText = text.toLowerCase();
return blacklist.some(keyword => {
    const lowerKeyword = keyword.trim().toLowerCase();
    return lowerKeyword && lowerText.includes(lowerKeyword);
});
```

---

## 错误处理机制

### 多层错误捕获

```javascript
// Phase 1 错误处理
try {
    const tagRegex = new RegExp(`<${escapeRegex(rule.value)}(?:\\s[^>]*)?>[\\s\\S]*?<\\/${escapeRegex(rule.value)}>`, 'gi');
    workingText = workingText.replace(tagRegex, '');
} catch (error) {
    console.error(`Error applying block exclusion rule:`, { rule, error });
}

// Phase 2 错误处理
try {
    if (rule.type === 'include') {
        results.push(...extractSimpleTag(workingText, rule.value));
        results.push(...extractCurlyBraceTag(workingText, rule.value));
    } else if (rule.type === 'regex_include') {
        const regex = new RegExp(rule.value, 'gi');
        // ...
    }
} catch (error) {
    console.error(`Error applying inclusion rule:`, { rule, error });
}

// Phase 3 错误处理
try {
    const regex = new RegExp(rule.value, 'gi');
    contentBlock = contentBlock.replace(regex, '');
} catch (error) {
    console.error(`Error applying cleanup rule:`, { rule, error });
}
```

### 未闭合标签警告

```javascript
// 检查标签闭合情况
const openTags = (text.match(new RegExp(`<${tag}>`, 'gi')) || []).length;
const closeTags = (text.match(new RegExp(`<\\/${tag}>`, 'gi')) || []).length;

if (openTags > closeTags) {
    console.warn(`警告: 发现 ${openTags - closeTags} 个未闭合的 <${tag}> 标签`);
}
```

---

## 扩展指南

### 添加新的规则类型

1. **在 TagRulesEditor.js 中添加选项**：

```html
<select class="rule-type text_pole widthUnset">
    <!-- 现有选项 -->
    <option value="new_type">新规则类型</option>
</select>
```

2. **在 tagExtractor.js 中添加处理逻辑**：

```javascript
export function extractTagContent(text, rules, blacklist = []) {
    // ... 现有代码 ...

    // 分类新规则类型
    const newTypeRules = rules.filter(rule => rule.type === 'new_type' && rule.enabled);

    // 在适当的阶段处理
    for (const rule of newTypeRules) {
        try {
            // 自定义处理逻辑
        } catch (error) {
            console.error(`Error applying new_type rule:`, { rule, error });
        }
    }

    // ... 现有代码 ...
}
```

### 添加新的标签格式支持

1. **创建新的提取函数**：

```javascript
/**
 * 提取新格式标签
 * @param {string} text - 搜索文本
 * @param {string} tag - 标签配置
 * @returns {string[]} 提取的内容数组
 */
export function extractNewFormatTag(text, tag) {
    const extractedContent = [];
    // 实现提取逻辑
    return extractedContent;
}
```

2. **集成到 extractTagContent**：

```javascript
if (rule.type === 'include') {
    results.push(...extractSimpleTag(workingText, rule.value));
    results.push(...extractCurlyBraceTag(workingText, rule.value));
    results.push(...extractNewFormatTag(workingText, rule.value)); // 新增
}
```

### 添加预设快捷操作

在设置HTML中添加按钮，在主JS文件中添加处理函数：

```javascript
// 添加"排除小CoT"快捷按钮处理
$('#vectors_enhanced_exclude_cot').on('click', () => {
    const settings = extension_settings.vectors_enhanced;
    if (!settings.selected_content.chat.tag_rules) {
        settings.selected_content.chat.tag_rules = [];
    }

    // 检查是否已存在
    const exists = settings.selected_content.chat.tag_rules.some(
        rule => rule.value === '<!--[\\s\\S]*?-->'
    );

    if (!exists) {
        settings.selected_content.chat.tag_rules.push({
            type: 'regex_exclude',
            value: '<!--[\\s\\S]*?-->',
            enabled: true
        });
        saveSettingsDebounced();
        renderTagRulesUI();
        toastr.success('已添加排除HTML注释规则');
    }
});
```

---

## 附录：完整规则处理流程伪代码

```
FUNCTION extractTagContent(text, rules, blacklist):
    IF rules is empty:
        RETURN text

    // 规则分类
    blockExcludeRules = FILTER rules WHERE type == 'exclude' AND enabled == true
    includeRules = FILTER rules WHERE type IN ['include', 'regex_include'] AND enabled == true
    cleanupRules = FILTER rules WHERE type == 'regex_exclude' AND enabled == true

    workingText = text

    // PHASE 1: 全局块级排除
    FOR EACH rule IN blockExcludeRules:
        TRY:
            regex = BUILD_REGEX_FOR_TAG(rule.value)
            workingText = workingText.replace(regex, '')
        CATCH error:
            LOG error

    // PHASE 2: 内容提取
    extractedContents = []

    IF includeRules.length > 0:
        FOR EACH rule IN includeRules:
            results = []
            TRY:
                IF rule.type == 'include':
                    results = CONCAT(
                        extractSimpleTag(workingText, rule.value),
                        extractCurlyBraceTag(workingText, rule.value)
                    )
                ELSE IF rule.type == 'regex_include':
                    regex = NEW_REGEXP(rule.value, 'gi')
                    matches = workingText.matchAll(regex)
                    FOR EACH match IN matches:
                        IF match[1] exists:
                            results.push(match[1])
            CATCH error:
                LOG error

            FOR EACH content IN results:
                extractedContents.push(content.trim())
    ELSE:
        extractedContents.push(workingText)

    // PHASE 3: 清理与黑名单
    finalContents = []

    FOR EACH contentBlock IN extractedContents:
        // 应用清理规则
        FOR EACH rule IN cleanupRules:
            TRY:
                regex = NEW_REGEXP(rule.value, 'gi')
                contentBlock = contentBlock.replace(regex, '')
            CATCH error:
                LOG error

        // 黑名单检查
        IF NOT shouldSkipContent(contentBlock, blacklist):
            finalContents.push(contentBlock)

    // 合并并清理
    result = finalContents.join('\n\n')
    result = result.replace(/\n\s*\n\s*\n/g, '\n\n')
    result = result.trim()

    RETURN result
```

---

**文档版本**: 1.0  
**创建日期**: 2026-03-09  
**适用版本**: vectors-enhanced v2.0+
